<view wx:if="{{showImg}}">
  <view class="modal" wx:if="{{showImg}}">
    <image src="../../img/userInfoAuth.png" mode="aspectFit"><button open-type="getUserInfo" bindgetuserinfo="getUserInfo" bindtap="click">授权</button></image>
  </view>
</view>

